{
  "name": "{{npmName}}",
  "version": "{{npmVersion}}",
  "description": "{{description}}",
  "author": "{{author}}",
  "license": "{{licenseName}}",
  "main": "dist/app/index.js",
  "scripts": {
    "init-install": "npm install",
    "dev": "tsx watch app/index.ts",
    "dev-mock": "OPENAPI_MOCK=true npm run dev",
    "build": "tsc",
    "start": "node dist/app/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@nestjs/common": "^{{nestVersion}}",
    "@nestjs/core": "^{{nestVersion}}",
    "@nestjs/platform-fastify": "^{{nestVersion}}",
    "ajv-formats": "^3.0.1",
    "fastify": "5.11.3",
    "js-yaml": "^5.3.0",
    "openapi-backend": "^5.20.0",
    "pino": "^10.3.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^{{rxjsVersion}}"
  },
  "devDependencies": {
    "@types/node": "^{{nodeVersion}}",
    "tsx": "^4.23.12",
    "typescript": "^{{{tsVersion}}}"
  },
  "engines": {
    "node": ">=22"
  }
}
